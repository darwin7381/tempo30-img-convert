# Railway Nixpacks 配置
# 用於指定系統依賴和建置設定

[phases.setup]
nixPkgs = ["python311", "libGL", "libglib", "libSM"]

[phases.install]
cmds = [
    "pip install --upgrade pip",
    "pip install -r requirements.txt",
    "echo '🔄 預下載 rembg AI 模型（176MB）...'",
    "python -c 'from rembg import remove; from PIL import Image; import numpy as np; img = Image.fromarray(np.zeros((100,100,3), dtype=np.uint8)); remove(img); print(\"✅ rembg 模型預下載成功\")'"
]

[start]
cmd = "uvicorn app:app --host 0.0.0.0 --port $PORT --timeout-keep-alive 120"

